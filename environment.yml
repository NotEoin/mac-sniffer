name: probe-sniffer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - scapy>=2.5.0
      - pyshark>=0.6
      - pytest>=8
      - matplotlib>=3.8   # only for tools/plot_capture.py
